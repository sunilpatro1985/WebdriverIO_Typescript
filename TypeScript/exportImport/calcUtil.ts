/*export const url = "https://qavalidation.com"

export const browserName = "chrome"*/

const url = "https://qavalidation.com"

const browserName = "chrome"

export const add = (a, b) =>{ //using arror function
    return a+b
}

  export function substract(a: number, b: number) {
    return a - b;
  }


  export default function multiply(a: number, b: number) {
    return a * b;
  }

  export {url, browserName};